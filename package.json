{
  "name": "sourcingbench-audit",
  "version": "1.0.0",
  "private": true,
  "description": "Public audit record for SourcingBench, the AI sourcing tool benchmark by SourcingTools.org. Run `npm run verify` to independently re-derive every published leaderboard.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify": "node scripts/verify-cycle.mjs --all",
    "check-links": "node scripts/check-links.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SourcingBench/SourcingTools.git"
  },
  "license": "MIT"
}
